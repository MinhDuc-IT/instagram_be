export type PostVisibility = 'public' | 'followers' | 'private';

export const POST_VISIBILITY = {
    PUBLIC: 'public' as PostVisibility,
    FOLLOWERS: 'followers' as PostVisibility,
    PRIVATE: 'private' as PostVisibility
};
